from setuptools import setup, find_packages

setup(
    name='msci_human_mob_project',
    version='0.1',
    long_description='Msci Human Mobility Project',
    packages=find_packages(),
    install_requires=[],
)